# AU/NZ 选品工具 - 设计文档

## 项目目标

构建一个针对澳大利亚和新西兰市场的电商选品工具，帮助卖家发现热销爆品、分析市场趋势、评估产品潜力。

## 核心功能

### 1. 多平台数据采集
| 平台 | 地区 | 数据获取方式 | 优先级 |
|------|------|-------------|--------|
| Amazon AU | 澳洲 | API (Keepa/SP-API) + 爬虫 | P0 |
| eBay AU | 澳洲 | eBay API | P0 |
| eBay NZ | 新西兰 | eBay API | P0 |
| TradeMe | 新西兰 | 爬虫 | P1 |
| Kogan | 澳洲 | 爬虫 | P2 |
| Catch | 澳洲 | 爬虫 | P2 |

### 2. Google 流量分析
- **Google Trends**: 分析关键词搜索趋势（AU/NZ 地区）
- **搜索量数据**: 关键词月搜索量、竞争度
- **季节性分析**: 识别产品的季节性需求波动
- **相关搜索词**: 发现潜在的长尾关键词机会

### 3. 爆品发现引擎
- 销量排名追踪
- 价格变动监控
- 评论数量/评分分析
- BSR (Best Seller Rank) 历史趋势
- 新品上升趋势检测

### 4. 产品分析维度
- **市场容量**: 预估月销量、市场规模
- **竞争程度**: 卖家数量、评论集中度
- **利润空间**: 价格区间、成本估算
- **趋势指标**: 上升/下降/稳定
- **Google 热度**: 搜索趋势、季节性

### 5. 数据可视化看板
- 品类热度地图
- 价格分布图
- 销量趋势图
- 竞争分析矩阵
- Google Trends 叠加图

### 6. 一键生成选品报告
自动汇总所有分析数据，生成专业的选品报告，支持多种格式导出和分享。

#### 报告类型
| 报告类型 | 适用场景 | 内容深度 |
|---------|---------|---------|
| 快速报告 | 日常选品决策 | 核心指标 + 简要分析 |
| 完整报告 | 深度分析/团队汇报 | 全维度数据 + 详细图表 |
| 对比报告 | 多产品/多品类对比 | 横向对比 + 推荐排序 |
| 监控报告 | 定期跟踪 | 变化趋势 + 异常提醒 |

#### 报告内容结构
```
📊 选品分析报告
├── 1. 报告概要
│   ├── 生成时间、分析周期
│   ├── 核心结论（1-3句话）
│   └── 综合评分与建议（推荐/观望/不推荐）
│
├── 2. 产品基础信息
│   ├── 产品名称、图片、链接
│   ├── 所属品类、关键词
│   └── 多平台价格对比表
│
├── 3. 市场分析
│   ├── 市场规模估算
│   ├── 月销量/收入预估
│   ├── 价格区间分布图
│   └── 季节性波动分析
│
├── 4. 竞争分析
│   ├── 竞争程度评级（低/中/高）
│   ├── 头部卖家分析（Top 10）
│   ├── 评论数量分布
│   ├── 新品存活率
│   └── 竞争格局图
│
├── 5. Google 流量分析
│   ├── 搜索趋势图（12个月）
│   ├── AU vs NZ 地区对比
│   ├── 月搜索量 & 竞争度
│   ├── 相关搜索词 Top 20
│   └── 季节性热度日历
│
├── 6. 趋势预测
│   ├── 历史趋势图
│   ├── 未来30天预测
│   ├── 趋势信号（上升/稳定/下降）
│   └── 最佳入场时机建议
│
├── 7. 利润估算
│   ├── 售价区间建议
│   ├── 预估成本（采购+物流+平台费）
│   ├── 毛利率计算
│   └── ROI 预估
│
├── 8. 风险评估
│   ├── 市场风险
│   ├── 竞争风险
│   ├── 季节性风险
│   └── 合规风险提示
│
└── 9. 选品建议
    ├── 综合评分明细
    ├── 优势与劣势总结
    ├── 可行性建议
    └── 下一步行动项
```

#### 导出格式
| 格式 | 用途 | 特点 |
|------|------|------|
| **PDF** | 分享/存档 | 精美排版、可打印 |
| **Excel** | 数据分析 | 原始数据、可编辑 |
| **在线链接** | 团队协作 | 实时更新、可评论 |
| **PPT** | 汇报演示 | 图表为主、简洁 |
| **Markdown** | 技术文档 | 纯文本、易集成 |

#### 报告分享功能
- **链接分享**: 生成唯一链接，设置有效期
- **密码保护**: 敏感报告可加密
- **团队空间**: 报告自动归档，支持搜索
- **定时发送**: 邮件/微信定时推送

## 技术架构

```
┌─────────────────────────────────────────────────────────────┐
│                        Frontend                              │
│                   (React + TypeScript)                       │
│  ┌─────────┐  ┌─────────┐  ┌─────────┐  ┌─────────┐        │
│  │ Dashboard│  │ Search  │  │ Analysis│  │ Alerts  │        │
│  └─────────┘  └─────────┘  └─────────┘  └─────────┘        │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                     Backend API                              │
│                   (Python FastAPI)                           │
│  ┌─────────┐  ┌─────────┐  ┌─────────┐  ┌─────────┐        │
│  │ Product │  │ Trend   │  │ Google  │  │ Alert   │  │ Report  │ │
│  │ Service │  │ Service │  │ Service │  │ Service │  │ Service │ │
│  └─────────┘  └─────────┘  └─────────┘  └─────────┘  └─────────┘ │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                    Supabase Platform                         │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐         │
│  │  PostgreSQL │  │   Storage   │  │    Auth     │         │
│  │  (数据库)    │  │ (文件存储)   │  │  (用户认证)  │         │
│  └─────────────┘  └─────────────┘  └─────────────┘         │
│  ┌─────────────┐  ┌─────────────┐                          │
│  │  Realtime   │  │    Redis    │                          │
│  │ (实时订阅)   │  │   (缓存)    │                          │
│  └─────────────┘  └─────────────┘                          │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                   Data Collection                            │
│  ┌──────────────────────────────────────────────────┐       │
│  │              Scheduler (Celery + Redis)           │       │
│  └──────────────────────────────────────────────────┘       │
│         │              │              │                      │
│         ▼              ▼              ▼                      │
│  ┌───────────┐  ┌───────────┐  ┌───────────┐               │
│  │  API      │  │  Scrapers │  │  Google   │               │
│  │  Clients  │  │(Playwright)│  │  Trends   │               │
│  │           │  │           │  │  Client   │               │
│  │ - eBay    │  │ - TradeMe │  │           │               │
│  │ - Keepa   │  │ - Kogan   │  │           │               │
│  │ - Amazon  │  │ - Catch   │  │           │               │
│  └───────────┘  └───────────┘  └───────────┘               │
└─────────────────────────────────────────────────────────────┘
```

## 技术选型

### 后端
- **框架**: Python FastAPI
- **任务队列**: Celery + Redis (或 Supabase Edge Functions)
- **爬虫**: Playwright (你已安装)
- **数据库**: Supabase (PostgreSQL)
- **文件存储**: Supabase Storage (报告文件)
- **用户认证**: Supabase Auth
- **实时更新**: Supabase Realtime (报告生成进度)

### 前端
- **框架**: React 18 + TypeScript
- **UI库**: Ant Design / Tailwind CSS
- **图表**: ECharts / Recharts
- **状态管理**: Zustand

### 数据获取
| 来源 | 方式 | 说明 |
|------|------|------|
| Amazon AU | Keepa API / SP-API | 需要订阅或开发者账号 |
| eBay AU/NZ | eBay Browse API | 免费额度有限 |
| TradeMe | Playwright 爬虫 | 公开页面数据 |
| Google Trends | pytrends 库 | 免费，有请求限制 |
| 关键词数据 | SerpAPI / DataForSEO | 付费API |

### 报告生成
| 功能 | 库/工具 | 说明 |
|------|--------|------|
| PDF生成 | WeasyPrint | HTML转PDF，支持CSS样式 |
| Excel生成 | openpyxl | 支持图表、样式 |
| PPT生成 | python-pptx | 幻灯片生成 |
| 图表渲染 | ECharts (服务端) | 使用 pyecharts 或 snapshot |
| AI结论 | Claude API / GPT | 可选，生成自然语言分析 |
| 文件存储 | Supabase Storage | 报告文件存储 |

## 数据模型

### Product (产品表)
```python
class Product:
    id: UUID
    platform: str          # amazon_au, ebay_au, trademe, etc.
    platform_id: str       # 平台内产品ID
    title: str
    category: str
    price: Decimal
    currency: str          # AUD, NZD
    rating: float
    review_count: int
    seller_count: int
    bsr_rank: int          # Best Seller Rank
    image_url: str
    product_url: str
    created_at: datetime
    updated_at: datetime
```

### PriceHistory (价格历史)
```python
class PriceHistory:
    id: UUID
    product_id: UUID
    price: Decimal
    recorded_at: datetime
```

### SalesEstimate (销量估算)
```python
class SalesEstimate:
    id: UUID
    product_id: UUID
    estimated_monthly_sales: int
    estimated_revenue: Decimal
    confidence: float
    recorded_at: datetime
```

### GoogleTrend (搜索趋势)
```python
class GoogleTrend:
    id: UUID
    keyword: str
    region: str            # AU, NZ
    interest: int          # 0-100
    related_queries: JSON
    recorded_at: datetime
```

### KeywordMetrics (关键词指标)
```python
class KeywordMetrics:
    id: UUID
    keyword: str
    region: str
    monthly_searches: int
    competition: float     # 0-1
    cpc: Decimal          # 广告点击成本
    trend: str            # rising, stable, declining
    recorded_at: datetime
```

### Report (选品报告)
```python
class Report:
    id: UUID
    title: str
    report_type: str       # quick, full, comparison, monitor
    status: str            # generating, completed, failed

    # 关联数据
    product_ids: List[UUID]
    keyword: str
    category: str

    # 报告内容 (JSON存储)
    summary: JSON          # 概要结论
    market_analysis: JSON  # 市场分析数据
    competition: JSON      # 竞争分析数据
    google_trends: JSON    # Google流量数据
    profit_estimate: JSON  # 利润估算
    risk_assessment: JSON  # 风险评估
    recommendation: JSON   # 选品建议

    # 评分
    overall_score: float   # 综合评分 0-100
    market_score: float
    competition_score: float
    trend_score: float
    profit_score: float

    # 导出
    pdf_url: str
    excel_url: str
    share_link: str
    share_password: str
    share_expires_at: datetime

    # 元数据
    created_by: UUID
    created_at: datetime
    updated_at: datetime
```

### ReportTemplate (报告模板)
```python
class ReportTemplate:
    id: UUID
    name: str
    description: str
    template_type: str     # quick, full, comparison
    sections: JSON         # 包含的章节配置
    styling: JSON          # 样式配置
    is_default: bool
    created_at: datetime
```

## 核心功能模块

### 模块1: 爆品发现
```
输入: 品类/关键词
处理:
  1. 从各平台获取 Top 100 产品
  2. 计算综合热度分数
  3. 交叉验证 Google 搜索趋势
  4. 排除季节性干扰
输出: 爆品列表 + 评分 + 趋势图
```

### 模块2: 竞争分析
```
输入: 产品/关键词
处理:
  1. 分析头部卖家占比
  2. 评论数量分布
  3. 价格区间分析
  4. 新品存活率
输出: 竞争难度评分 + 建议
```

### 模块3: 趋势预测
```
输入: 品类/产品
处理:
  1. 历史销量趋势
  2. Google Trends 数据
  3. 季节性因子
  4. 机器学习预测
输出: 未来30天趋势预测
```

### 模块4: 选品评分
```
综合评分 =
  市场容量(25%) +
  竞争程度(25%) +
  利润空间(20%) +
  趋势指标(15%) +
  Google热度(15%)
```

### 模块5: 一键报告生成
```
输入: 产品ID/关键词/品类 + 报告类型
处理:
  1. 并行获取所有相关数据
     ├── 产品基础信息
     ├── 多平台价格数据
     ├── 销量历史数据
     ├── Google Trends 数据
     └── 竞品数据
  2. 运行分析算法
     ├── 市场规模计算
     ├── 竞争度评估
     ├── 利润空间估算
     └── 趋势预测
  3. 生成综合评分
  4. AI 生成文字结论
  5. 渲染报告模板
  6. 导出多格式文件
输出: 完整选品报告 + 下载链接 + 分享链接
```

#### 报告生成流程
```
┌─────────────────────────────────────────────────────────────┐
│                    用户触发报告生成                          │
│         (单品报告 / 批量报告 / 定时报告)                      │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                     任务队列 (Celery)                        │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  Report Task: 异步执行，支持进度回调                   │   │
│  └─────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
                              │
          ┌───────────────────┼───────────────────┐
          ▼                   ▼                   ▼
   ┌─────────────┐     ┌─────────────┐     ┌─────────────┐
   │ 数据采集层   │     │ 数据采集层   │     │ 数据采集层   │
   │             │     │             │     │             │
   │ 产品数据    │     │ Google数据  │     │  竞品数据   │
   └─────────────┘     └─────────────┘     └─────────────┘
          │                   │                   │
          └───────────────────┼───────────────────┘
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                      分析引擎                                │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐      │
│  │市场分析   │ │竞争分析   │ │利润估算   │ │趋势预测   │      │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘      │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                    AI 结论生成 (可选)                        │
│           使用 LLM 生成自然语言分析结论                       │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                     报告渲染引擎                             │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐      │
│  │ PDF生成  │ │ Excel生成 │ │ PPT生成  │ │ Web预览  │      │
│  │(WeasyPrint)│ │(openpyxl)│ │(pptx)   │ │ (React) │      │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘      │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                   Supabase Storage                           │
│          (报告文件存储，支持公开/私有访问控制)                  │
└─────────────────────────────────────────────────────────────┘
```

#### 报告 API 接口
```python
# 生成报告
POST /api/reports/generate
{
    "type": "full",                    # quick/full/comparison/monitor
    "target": {
        "product_ids": ["uuid1"],      # 或
        "keyword": "wireless earbuds", # 或
        "category": "Electronics"
    },
    "options": {
        "include_competitors": true,
        "forecast_days": 30,
        "export_formats": ["pdf", "excel"]
    }
}

# 查询生成进度
GET /api/reports/{report_id}/status
Response: {
    "status": "generating",
    "progress": 65,
    "current_step": "分析竞争数据..."
}

# 获取报告
GET /api/reports/{report_id}

# 下载报告
GET /api/reports/{report_id}/download?format=pdf

# 生成分享链接
POST /api/reports/{report_id}/share
{
    "expires_in": 7,        # 天
    "password": "optional"
}
```

## 页面设计

### 1. Dashboard (首页看板)
- 今日热门品类
- 上升趋势产品 Top 10
- 各平台数据概览
- Google 热搜关键词

### 2. 产品搜索页
- 多平台搜索
- 筛选器: 价格、评分、销量、平台
- 排序: 热度、上升速度、利润率

### 3. 产品详情页
- 基础信息
- 价格历史曲线
- 销量趋势
- 竞品对比
- Google Trends 叠加

### 4. 品类分析页
- 品类热度排名
- 子品类钻取
- 竞争格局
- 入场建议

### 5. 关键词工具
- Google Trends 查询
- 搜索量分析
- 相关词挖掘
- 季节性日历

### 6. 监控告警
- 价格变动提醒
- 新爆品提醒
- 竞品动态
- 趋势转折点

### 7. 报告中心页
- **快速生成入口**: 一键生成当前产品/品类报告
- **报告列表**: 历史报告管理、搜索、筛选
- **模板管理**: 自定义报告模板
- **批量生成**: 多产品批量生成报告
- **定时任务**: 设置每周/每月自动生成报告
- **报告预览**: 在线预览，支持全屏
- **下载中心**: 多格式下载、批量打包
- **分享管理**: 查看分享链接、访问统计

#### 报告生成界面交互
```
┌────────────────────────────────────────────────────────────┐
│  📊 生成选品报告                                     [X]   │
├────────────────────────────────────────────────────────────┤
│                                                            │
│  分析目标:  ○ 单个产品  ○ 关键词  ● 品类                    │
│                                                            │
│  品类选择:  [Electronics > Audio > Earbuds      ▼]        │
│                                                            │
│  报告类型:  ┌──────────┐ ┌──────────┐ ┌──────────┐        │
│            │ 📋 快速   │ │ 📊 完整  │ │ 🔄 对比  │        │
│            │  报告    │ │  报告 ✓ │ │  报告    │        │
│            └──────────┘ └──────────┘ └──────────┘        │
│                                                            │
│  报告选项:                                                  │
│    ☑ 包含竞品分析 (Top 10)                                 │
│    ☑ 包含 Google 流量分析                                  │
│    ☑ 包含趋势预测 (30天)                                   │
│    ☐ AI 智能结论生成                                       │
│                                                            │
│  导出格式:  ☑ PDF  ☑ Excel  ☐ PPT  ☐ Markdown             │
│                                                            │
├────────────────────────────────────────────────────────────┤
│                           [取消]  [🚀 生成报告]            │
└────────────────────────────────────────────────────────────┘
```

#### 报告生成进度
```
┌────────────────────────────────────────────────────────────┐
│  📊 正在生成报告...                                        │
├────────────────────────────────────────────────────────────┤
│                                                            │
│  ████████████████░░░░░░░░░░░░░░  45%                      │
│                                                            │
│  ✓ 获取产品数据                                            │
│  ✓ 获取价格历史                                            │
│  ● 分析 Google 趋势...                                     │
│  ○ 竞争分析                                                │
│  ○ 利润估算                                                │
│  ○ 生成报告文件                                            │
│                                                            │
│  预计剩余: 约 30 秒                                        │
│                                                            │
└────────────────────────────────────────────────────────────┘
```

## 实现计划

### Phase 1: 基础架构 (MVP)
- [ ] 项目初始化、数据库设计
- [ ] eBay API 集成 (AU/NZ)
- [ ] 基础爬虫框架 (TradeMe)
- [ ] Google Trends 集成
- [ ] 简单 Web 界面

### Phase 2: 数据丰富
- [ ] Amazon AU 数据集成
- [ ] 价格历史追踪
- [ ] 销量估算算法
- [ ] 关键词搜索量 API

### Phase 3: 分析功能
- [ ] 爆品发现算法
- [ ] 竞争分析模块
- [ ] 选品评分系统
- [ ] 数据可视化增强

### Phase 4: 报告生成系统
- [ ] 报告数据模型设计
- [ ] 报告生成任务队列
- [ ] PDF 报告渲染 (WeasyPrint)
- [ ] Excel 报告导出 (openpyxl)
- [ ] 报告模板系统
- [ ] 分享链接功能
- [ ] 报告中心页面

### Phase 5: 高级功能
- [ ] AI 智能结论生成
- [ ] 趋势预测模型
- [ ] 智能推荐
- [ ] 告警系统
- [ ] 批量报告生成
- [ ] 定时报告任务

## Supabase 集成

### 数据库表结构
```sql
-- 产品表
CREATE TABLE products (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    platform VARCHAR(50) NOT NULL,
    platform_id VARCHAR(100) NOT NULL,
    title TEXT NOT NULL,
    category VARCHAR(200),
    price DECIMAL(10,2),
    currency VARCHAR(3) DEFAULT 'AUD',
    rating DECIMAL(2,1),
    review_count INTEGER DEFAULT 0,
    seller_count INTEGER DEFAULT 1,
    bsr_rank INTEGER,
    image_url TEXT,
    product_url TEXT,
    created_at TIMESTAMPTZ DEFAULT NOW(),
    updated_at TIMESTAMPTZ DEFAULT NOW(),
    UNIQUE(platform, platform_id)
);

-- 报告表
CREATE TABLE reports (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    user_id UUID REFERENCES auth.users(id),
    title VARCHAR(200) NOT NULL,
    report_type VARCHAR(20) NOT NULL,
    status VARCHAR(20) DEFAULT 'pending',
    progress INTEGER DEFAULT 0,

    -- 分析数据 (JSONB)
    summary JSONB,
    market_analysis JSONB,
    competition JSONB,
    google_trends JSONB,
    profit_estimate JSONB,
    risk_assessment JSONB,
    recommendation JSONB,

    -- 评分
    overall_score DECIMAL(4,1),

    -- 文件路径 (Supabase Storage)
    pdf_path TEXT,
    excel_path TEXT,

    -- 分享
    share_token VARCHAR(64) UNIQUE,
    share_password VARCHAR(100),
    share_expires_at TIMESTAMPTZ,

    created_at TIMESTAMPTZ DEFAULT NOW(),
    updated_at TIMESTAMPTZ DEFAULT NOW()
);

-- 启用 RLS (行级安全)
ALTER TABLE reports ENABLE ROW LEVEL SECURITY;
CREATE POLICY "Users can view own reports" ON reports
    FOR SELECT USING (auth.uid() = user_id);
```

### Storage Buckets
```
reports/              -- 报告文件存储
  ├── pdf/           -- PDF 报告
  ├── excel/         -- Excel 报告
  └── images/        -- 报告中的图表截图
```

### 实时进度推送
```typescript
// 前端监听报告生成进度
const channel = supabase
  .channel('report-progress')
  .on('postgres_changes', {
    event: 'UPDATE',
    schema: 'public',
    table: 'reports',
    filter: `id=eq.${reportId}`
  }, (payload) => {
    setProgress(payload.new.progress)
    setStatus(payload.new.status)
  })
  .subscribe()
```

### Python SDK 示例
```python
from supabase import create_client

supabase = create_client(SUPABASE_URL, SUPABASE_KEY)

# 保存报告
report = supabase.table('reports').insert({
    'title': '无线耳机选品分析',
    'report_type': 'full',
    'status': 'generating'
}).execute()

# 上传 PDF 到 Storage
with open('report.pdf', 'rb') as f:
    supabase.storage.from_('reports').upload(
        f'pdf/{report_id}.pdf', f
    )

# 更新进度 (触发 Realtime)
supabase.table('reports').update({
    'progress': 50,
    'status': 'analyzing'
}).eq('id', report_id).execute()
```

## API 参考

### eBay Browse API
```
GET /buy/browse/v1/item_summary/search
- 免费额度: 5000次/天
- 文档: https://developer.ebay.com/api-docs/buy/browse/overview.html
```

### Google Trends (pytrends)
```python
from pytrends.request import TrendReq
pytrends = TrendReq(hl='en-AU', tz=600)
pytrends.build_payload(['keyword'], geo='AU')
```

### TradeMe (爬虫)
```
Base URL: https://www.trademe.co.nz
需要处理: 登录态、反爬、分页
```

## 成本估算

| 项目 | 费用 | 说明 |
|------|------|------|
| Supabase | 免费 / $25/月 | Free tier 足够开发，Pro 用于生产 |
| eBay API | 免费 | 有请求限制 |
| Keepa API | ~$20/月 | Amazon 数据 |
| SerpAPI | ~$50/月 | 关键词数据 |
| 服务器 | ~$20/月 | VPS 运行爬虫和任务 |
| AI结论生成 | ~$10/月 | Claude/GPT API (可选) |
| **总计** | **~$100-125/月** | 可从免费开始 |

### Supabase 方案优势
- **免费额度**: 500MB 数据库 + 1GB 存储 + 50K 月活用户
- **一站式**: 数据库 + 存储 + 认证 + 实时订阅
- **简化部署**: 无需自己管理数据库服务器
- **前端友好**: 提供 JavaScript/Python SDK

## 风险与对策

| 风险 | 对策 |
|------|------|
| 爬虫被封 | IP轮换、请求限速、Playwright模拟 |
| API限额 | 数据缓存、增量更新 |
| 数据准确性 | 多源交叉验证 |
| 法律合规 | 仅采集公开数据，遵守robots.txt |

## 下一步

1. 确认设计方案
2. 初始化项目结构
3. 从 eBay API + Google Trends 开始 MVP
4. 完成基础报告生成功能 (PDF/Excel)
